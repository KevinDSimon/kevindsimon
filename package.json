{
  "scripts": {
    "dev": "next",
    "build": "next build",
    "start": "next start",
    "export": "next export",
    "deploy": "next build && next export && cp -R ./CNAME ./out/ && cp -R ./robots.txt ./out/ && git add out/ && git commit -m \"Deploy gh-pages\" && git subtree push --prefix out origin gh-pages"
  },
  "dependencies": {
    "next": "^6.1.1",
    "react": "^16.4.2",
    "react-dom": "^16.4.2",
    "react-scroll": "^1.7.6",
    "react-scroll-parallax": "^1.2.1",
    "react-swipeable-views": "^0.12.16",
    "react-waypoint": "^8.0.0"
  }
}
