{
  "scripts": {
    "dev": "next",
    "build": "next build",
    "start": "next start",
    "export": "next export",
    "deploy": "next build && next export -o docs && git checkout -- docs/CNAME && git checkout -- docs/robots.txt && git checkout -- docs/.nojekyll && git add docs && git commit -m \"Deploy: Regenerate Site\" && git push origin master"
  },
  "dependencies": {
    "next": "^6.1.1",
    "react": "^16.4.2",
    "react-dom": "^16.4.2",
    "react-scroll": "^1.7.6",
    "react-scroll-parallax": "^1.2.1",
    "react-swipeable-views": "^0.12.16",
    "react-waypoint": "^8.0.0"
  }
}
